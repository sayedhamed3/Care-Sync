<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/profile-show.css">
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <div id="main-container">
        <h1>Profile</h1>
        <div id="doctor-details">
            <h3>Doctor Details</h3>
            <p><%= doctor.name %></p>
            <p><%= doctor.specialization %></p>

            <h3>Contact Information</h3>
            <p><%= doctor.email %></p>
            <p><%= doctor.phone %></p>
        </div>
        <div id="buttons-container">
            <a id="edit-doctor-button" href="/auth/profile/edit">Edit Profile</a>
        
            <form action="/auth/<%= doctor._id %>/delete?_method=PUT" method="POST">
                <button id="delete-doctor-button">Delete Account</button>
            </form>
        </div>
    </div>
</body>
</html>